namespace LP2_P2
{
    /// <summary>
    /// Defines which type of object it is
    /// </summary>
    public enum ObjectType
    {
        emptySpace,
        teleporter,
        wall,
        player,
        ghost,
        pellet,
        bigPellet,
        target,
        door
    }
}
